<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="com.winter.app.product.ProductDAO">
  
<!--   	<sql id="search">
  	WHERE PRODUCTCONTENTS = '%'||#{search}||'%'
  	</sql>
  
  	<sql id="search2">
  	WHERE
  	<if test="kind='kind1'">
  		PRODUCTNAME LIKE = '%'||#{search}||'%'
  	</if>
  	<if test="kind='kind2'">
  		PRODUCTCONTENTS LIKE = '%'||#{search}||'%'
  	</if>
  	<if test="kind='kind3'">
  		PRODUCTRATE LIKE = '%'||#{search}||'%'
  	</if>
  	<if test="kind='kind4'">
  		PRODUCTJUMSU LIKE = '%'||#{search}||'%'
  	</if>
  	</sql>
   -->
  
  	<select id="list" resultType="ProductDTO" parameterType="Pager">
  		SELECT * FROM 
		(SELECT ROWNUM R, PR.* FROM
		(SELECT * FROM PRODUCT 
		
		
		ORDER BY PRODUCTNUM DESC) PR) 
		WHERE R BETWEEN #{startRow} AND #{lastRow} 
  	</select>
  	
  	<select id="detail" resultMap="detailResult" parameterType="ProductDTO">
  		SELECT * FROM PRODUCT LEFT OUTER JOIN PRODUCTFILES USING (PRODUCTNUM) WHERE PRODUCTNUM=#{productNum}
  	</select>	
  	
  	<resultMap type="ProductDTO" id="detailResult">
  		<id column="PRODUCTNUM" property="productNum"/>
  		<result column="PRODUCTNAME" property=""/>
  		<result column="PRODUCTCONTENTS" property="productContents"/>
  	 	<result column="PRODUCTRATE" property="productRate"/>
  	 	<result column="PRODUCTJUMSU" property="productJumsu"/>
  	 	 	
  	 	 <association property="productFileDTO" javaType="ProductFileDTO">
  	 	 <id column="FILENUM" property="fileNum"/>
  	 	 <result column="FILENAME" property="fileName"/>
  	 	 <result column="ORINAME" property="oriName"/>
  	 	 </association>
  	</resultMap>
  		
  		

  
  	<insert id="add" parameterType="ProductDTO">
  		<selectKey keyProperty="productNum" order="BEFORE" resultType="Long">
  		SELECT PRODUCT_SEQ.NEXTVAL FROM DUAL
  		</selectKey>
  		INSERT INTO PRODUCT VALUES (#{productNum},#{productName},#{productContents},#{productRate},#{productJumsu})
  	</insert>
  
  
  
  	<insert id="addFile" parameterType="ProductFileDTO">
  		INSERT INTO PRODUCTFILES VALUES
  		(PRODUCT_SEQ.NEXTVAL,#{productNum},#{fileName},#{oriName}) 
  	</insert>
  
  	<select id="getListFile" parameterType="productNum" resultType="ProductFileDTO">
  		SELECT * FROM PRODUCTFILES WHERE PRODUCTNUM = #{productNum}
  	</select>
  
  
  
  	<delete id="delete" parameterType="ProductDTO">
  		DELETE PRODUCT WHERE PRODUCTNUM=#{productNum}
  	</delete>
  	
  	<update id="update" parameterType="ProductDTO">
  		UPDATE PRODUCT SET PRODUCTNAME=#{productName},
  		PRODUCTCONTENTS=#{productContents},PRODUCTRATE=#{productRate},
  		PRODUCTJUMSU=#{productJumsu} WHERE PRODUCTNUM=#{productNum}
  	</update>
  	
  	<select id="total" resultType="Long">
  		SELECT COUNT(PRODUCTNUM) FROM PRODUCT
  	</select>
  	
  	
  </mapper>